import 'package:flutter/material.dart';

class AppPallete {
  // Primary Colors (Light Mode)
  static const Color primaryLight = Color(0xFF1E88E5); // Blue
  static const Color primaryVariantLight = Color(0xFF1565C0); // Darker Blue

  // Primary Colors (Dark Mode)
  static const Color primaryDark = Color(0xFF90CAF9); // Lighter Blue for Dark Mode
  static const Color primaryVariantDark = Color(0xFF42A5F5); // Darker Blue for Dark Mode

  // Secondary Colors (Light Mode)
  static const Color secondaryLight = Color(0xFF00E676); // Green
  static const Color secondaryVariantLight = Color(0xFF00C853); // Darker Green

  // Secondary Colors (Dark Mode)
  static const Color secondaryDark = Color(0xFF76FF03); // Lighter Green for Dark Mode
  static const Color secondaryVariantDark = Color(0xFF64DD17); // Darker Green for Dark Mode

  // Tertiary Colors
  static const Color tertiaryDark = Color(0xFFFFD54F); // Lighter Amber for Dark Mode

  // Error Colors (Light Mode)
  static const Color errorLight = Color(0xFFD32F2F); // Red
  static const Color errorVariantLight = Color(0xFFB71C1C); // Dark Red

  // Error Colors (Dark Mode)
  static const Color errorDark = Color(0xFFEF5350); // Lighter Red for Dark Mode

  // Background and Surface (Light Mode)
  static const Color backgroundLight = Color(0xFFF5F5F5); // Light Gray
  static const Color surfaceLight = Color(0xFFFFFFFF); // White

  // Background and Surface (Dark Mode)
  static const Color backgroundDark = Color(0xFF303030); // Dark Gray for Dark Mode
  static const Color surfaceDark = Color(0xFF424242); // Dark Surface for Dark Mode

  // Text and Icons (Light Mode)
  static const Color onPrimaryLight = Color(0xFFFFFFFF); // White for primary
  static const Color onSecondaryLight = Color(0xFF000000); // Black for secondary
  static const Color onBackgroundLight = Color(0xFF212121); // Dark Text
  static const Color onSurfaceLight = Color(0xFF424242); // Medium-dark text
  static const Color onErrorLight = Color(0xFFFFFFFF); // White for error

  // Text and Icons (Dark Mode)
  static const Color onPrimaryDark = Color(0xFF000000); // Black for primary on dark mode
  static const Color onSecondaryDark = Color(0xFF000000); // Black for secondary on dark mode
  static const Color onBackgroundDark = Color(0xFFE0E0E0); // Light text for background on dark mode
  static const Color onSurfaceDark = Color(0xFFF5F5F5); // Light text for surface on dark mode
  static const Color onErrorDark = Color(0xFF000000); // Black for error on dark mode

  // Disabled and Hint Colors (Light Mode)
  static const Color disabledLight = Color(0xFFBDBDBD); // Light Gray
  static const Color hintLight = Color(0xFF9E9E9E); // Medium Gray

  // Disabled and Hint Colors (Dark Mode)
  static const Color disabledDark = Color(0xFF616161); // Darker disabled color
  static const Color hintDark = Color(0xFF757575); // Darker hint color

  // Other Utility Colors (Light Mode)
  static const Color successLight = Color(0xFF4CAF50); // Success Green
  static const Color warningLight = Color(0xFFFF9800); // Warning Orange
  static const Color infoLight = Color(0xFF03A9F4); // Info Light Blue

  // Other Utility Colors (Dark Mode)
  static const Color successDark = Color(0xFF388E3C); // Darker Success Green
  static const Color warningDark = Color(0xFFFFB300); // Darker Warning Orange
  static const Color infoDark = Color(0xFF0288D1); // Darker Info Light Blue

  // Divider and Border Colors (Light Mode)
  static const Color dividerLight = Color(0xFFBDBDBD); // Divider Gray
  static const Color borderLight = Color(0xFFE0E0E0); // Light Border Gray

  // Divider and Border Colors (Dark Mode)
  static const Color dividerDark = Color(0xFF616161); // Divider Gray for dark mode
  static const Color borderDark = Color(0xFF616161); // Border Gray for dark mode
}
